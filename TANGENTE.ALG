algoritmo "tangente"
var
   n1,resultado:real
inicio
   escreval("insira o numero que deseja saber a tangente:")
   leia(n1)
   
   resultado <- tan(n1)
   
   escreval("A tangente de ", n1, " e ",resultado)

fimalgoritmo